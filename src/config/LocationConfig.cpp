#include "LocationConfig.hpp"

LocationConfig::LocationConfig()
: path(""), 
    root(""), 
    methods(),
    autoindex(false), 
    upload_store(""), 
    redirectCode(0), 
    redirectUrl(""), 
    index(""), 
    cgi() 
{
    
}

